Format, lint, and commit all changes:

1. Run `ruff format` on changed files in `src/` and `tests/`
2. Run `ruff check --fix` on `src/` and `tests/`
3. Run `git diff` and `git status` to review changes
4. Run `git add -A`
5. Commit with a descriptive message
6. Run `git push origin main`
